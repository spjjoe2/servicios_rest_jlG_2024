server.port=8000
server.servlet.context-path=/cursos
spring.application.name=servicio-cursos
eureka.client.service-url.defaultZone=http:${URL_EUREKA}
eureka.instance.hostname=${IP_HOST}
eureka.instance.non-secure-port=${PORT_HOST}